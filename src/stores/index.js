import { createPinia } from "pinia";
// La fonction createPinia() est utilisée pour créer une nouvelle instance du magasin, qui est ensuite exportée depuis le module.
const pinia = createPinia()

export default pinia